{% set new = alertList|length %}
<li class="dropdown notifications-menu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <i class="fa fa-exclamation-triangle"></i>
        {% if new > 0 %}
            <span class="label label-danger">{{ new }}</span>
        {% endif %}
    </a>
    <ul class="dropdown-menu">
        <li class="header">Vous avez {{ new }} nouvelles alertes</li>
        <li>
            <!-- inner menu: contains the actual data -->
            <ul class="menu">
                {% for alert in alertList %}
                    <li>
                        {% include 'Dywee' ~ alert.bundle|capitalize ~ 'Bundle:Alert:' ~ alert.type ~ '.html.twig' %}
                    </li>
                {% endfor %}
                {#<li>
                    <a href="#">
                        <i class="fa fa-warning text-yellow"></i> Very long description here that may not fit into the page and may cause design problems
                    </a>
                </li>
                <li>
                    <a href="#">
                        <i class="fa fa-users text-red"></i> 5 new members joined
                    </a>
                </li>

                <li>
                    <a href="#">
                        <i class="fa fa-shopping-cart text-green"></i> 25 sales made
                    </a>
                </li>
                <li>
                    <a href="#">
                        <i class="fa fa-user text-red"></i> You changed your username
                    </a>
                </li>#}
            </ul>
        </li>
        <li class="footer"><a href="{{ path('dywee_alert_table') }}">Voir toutes les alertes</a></li>
    </ul>
</li>